# 設計器版本化與唯讀路徑需求

- 日期：2026-09-18
- 來源：使用者對現行設計器進入路徑的修正
- 狀態：**已記錄，尚未實作**
- 相關：[D-07](02_決議紀錄.md)（沙箱與模擬簽核）、
  [表單流程異動治理需求](03_表單流程異動治理需求.md)

---

## 1. 使用者的需求原文

> 我想要的程式路徑是！表單設計器、程式設計器等等！
> List 只要顯示每個表單或流程當前使用的版本！
> 點選某個表單或流程後跟目前一樣顯示編輯畫面，但是為唯讀！
> 上半部增加一個開發的按鈕！點選開發按鈕後顯示一個清單！
> 顯示當前表單或流程的所有版本！點選後才能編輯表單與流程！
> 編輯後的儲存按鈕分兩個！一個是儲存！一個是另存新版。
> 但是完全新增一個表單或流程的版本就是直接打開編輯！
> 編輯頁面也要包含現在委托另外一台開發的模擬功能！

---

## 2. 為什麼要改

### 2.1 現況的問題

目前點進設計器就是可編輯狀態。這有兩個問題：

**看與改沒有分界。** 使用者想確認「這張表單現在長什麼樣」時，
一進去就處於隨時可能誤改的狀態。手滑動到某個欄位，
按了存檔就改變了定義。

**版本不可見。** 資料結構是 append-only、所有歷史版本都在，
但畫面上看不到——使用者不知道有版本這件事，
也無從得知「上個月的版本是什麼樣」。

### 2.2 改動後的路徑

```
清單（只顯示當前版本）
   │
   ▼  點選
唯讀檢視（與編輯畫面相同，但不能改）
   │
   ▼  按「開發」
版本清單（該表單／流程的所有版本）
   │
   ▼  選一個版本
編輯模式（可改、可模擬、可存檔）
```

新增表單／流程時**跳過唯讀與版本清單，直接進編輯**——
新的東西沒有歷史可看。

### 2.3 這個設計的意義

**預設安全。** 誤改的成本高於多按一次的成本。
「要編輯就明確表示」比「隨時都能改」適合把 Owner 交給使用者的產品。

「開發」這個詞是使用者選的，與 [D-07](02_決議紀錄.md) 的
「編輯開發模式」一致——兩者指的是同一件事的兩個面向：
D-07 講的是資料隔離（沙箱），本文講的是進入路徑。

---

## 3. 各段的需求

### 3.1 清單：只顯示當前版本

**現況**：清單已顯示 `published_version` 與 `has_draft`
（`FormList.vue`、`WorkflowList.vue`，2026-09-18 完成）。

**要調整的**：

| 項目 | 現況 | 需求 |
|------|------|------|
| 顯示內容 | `v3`、`v2 · 有草稿`、`尚未發布` | 保持，語意已符合 |
| 點選行為 | 進入**可編輯**的設計器 | 進入**唯讀檢視** |

只有點選行為要改。

### 3.2 唯讀檢視

**與編輯畫面相同的版面**，但：

| 元素 | 唯讀時的行為 |
|------|-------------|
| 畫布 | 可以點選欄位／節點查看屬性 |
| 屬性面板 | 顯示所有設定，全部不可修改 |
| 元件庫／節點庫 | 隱藏或停用（不能加東西）|
| 儲存／發布按鈕 | 隱藏 |
| 驗證按鈕 | **保留**——檢視時確認合法性沒有壞處 |
| 權限預覽 | **保留**——那是唯讀操作 |
| **開發按鈕** | 新增。唯一的編輯入口 |

**要明確標示處於唯讀。** 使用者要看得出為什麼改不動，
而不是以為系統壞了。

技術上：`FormDesigner.vue` 與 `WorkflowDesigner.vue` 要支援
`readonly` 模式。這是既有元件的擴充，不是新頁面——
兩份版面必然漂移。

### 3.3 版本清單

按「開發」後顯示。

**要顯示的**：

| 欄位 | 說明 |
|------|------|
| 版本號 | v1、v2、v3… |
| 狀態 | 草稿／已發布／已封存 |
| 建立者、建立時間 | 誰在什麼時候改的 |
| 發布者、發布時間 | 已發布的版本才有 |
| 摘要 | 與前一版的差異（見 §5.2，可延後）|

**操作**：

- 點選版本 → 進入編輯模式
- 當前發布版本要有明顯標示

**後端已完備**：

```
GET /forms/{form_key}/versions
GET /forms/{form_key}/versions/{version}
GET /workflows/{key}/versions
GET /workflows/{key}/versions/{version}
```

**前端缺一半**：`web/src/api/forms.ts` 有 `listVersions` 與
`getVersion`，但 `web/src/api/workflows.ts` **沒有**——要補上。

### 3.4 編輯模式的兩個儲存按鈕

| 按鈕 | 行為 | 用在什麼情況 |
|------|------|-------------|
| **儲存** | 覆寫當前草稿 | 還在同一輪修改中 |
| **另存新版** | 建立新的版本 | 改動大到該留下分界 |

#### ⚠️ 這裡有一個資料庫約束要先解決

`server/migrations/0002_form_definition.sql:62`：

```sql
-- 每個表單最多一份草稿
create unique index form_version_single_draft
    on form_definition_version (form_id)
    where status = 'DRAFT';
```

**每個表單只能有一份草稿。** 所以「另存新版」不能簡單理解成
「再建一份草稿」——那會違反約束。

三種可能的語意，需要決定：

| 選項 | 「另存新版」的行為 | 代價 |
|------|-------------------|------|
| **A** | 把當前草稿發布成新版本，然後開一份新草稿 | 等於「發布 + 繼續改」。但使用者可能不想現在發布 |
| **B** | 放寬約束允許多份草稿，用版本號區分 | 要處理「哪一份是主要草稿」；`get_draft()` 回傳單一值的假設會壞掉 |
| **C** | 另存新版 = 封存當前草稿，以其內容開新草稿 | 保持單一草稿約束，歷史也留下了 |

**傾向 C**。它不動既有約束，語意也接近使用者的直覺
（「把現在這版留著，接下來改的是新的一版」）。
但需要 `ARCHIVED` 狀態能容納未發布的內容——
目前約束 `status <> 'DRAFT' or version is null` 暗示
非草稿必須有版本號，要確認 `ARCHIVED` 的處理。

**這項要與使用者確認再實作。**

### 3.5 新增時直接進編輯

**現況已符合**：`FormList.vue` 與 `WorkflowList.vue` 建立成功後
直接 `router.push` 到設計器。

只要確認新增後進的是**編輯模式**而非唯讀模式即可。
實作時若用網址參數區分模式（例如 `?mode=edit`），
新增後要帶上那個參數。

### 3.6 編輯頁面整合模擬功能

**這部分等另一台機器完成 S2 模擬簽核後再整合。**

依 [總體實踐規劃](../../實踐規劃/規劃/202609/00_總體實踐規劃.md)，
模擬簽核依賴 P3 → S0 → S1 → S2，估計還有約 3 週。

整合時要決定的：

| 項目 | 說明 |
|------|------|
| 入口位置 | 編輯畫面的哪裡放「模擬」按鈕 |
| 模擬的對象 | 當前編輯中的草稿，還是要先存檔 |
| 未存檔的改動 | 模擬前強制存檔，或允許模擬記憶體中的版本 |
| 回到編輯 | 模擬完成後如何回來繼續改 |

**建議：模擬前強制存檔。** 模擬會建立沙箱租戶並跑真實流程，
拿一份記憶體中的草稿去跑，出問題時無從追溯用的是哪個版本。

---

## 4. 對現有程式的影響

### 4.1 要改的檔案

| 檔案 | 改動 |
|------|------|
| `web/src/router.ts` | 設計器路由要能區分唯讀／編輯／版本清單 |
| `web/src/pages/FormDesigner.vue` | 支援 `readonly` 模式 |
| `web/src/pages/WorkflowDesigner.vue` | 同上 |
| `web/src/pages/FormList.vue` | 點選改為進唯讀 |
| `web/src/pages/WorkflowList.vue` | 同上 |
| `web/src/api/workflows.ts` | **補 `listVersions` 與 `getVersion`** |
| 新增版本清單畫面 | 表單與流程共用一個元件 |

### 4.2 路由設計（初步）

```
/designer/forms                          清單
/designer/forms/:formKey                 唯讀檢視
/designer/forms/:formKey/versions        版本清單
/designer/forms/:formKey/edit            編輯當前草稿
/designer/forms/:formKey/edit/:version   編輯指定版本
```

流程比照。

**注意**：`/designer/forms/:formKey` 目前直接是編輯畫面，
改成唯讀是**破壞性變更**——`auth-navigation.spec.ts` 與
截圖腳本都會失敗。那是預期的，測試釘住的是舊行為。

### 4.3 不影響的部分

- 後端 API 完全不用改（版本端點已存在）
- 資料結構不用改（除了 §3.4 的草稿約束待決）
- 權限判定不用改

---

## 5. 尚未決定

| # | 問題 | 備註 |
|---|------|------|
| 1 | **「另存新版」的確切語意** | 見 §3.4 的三個選項，傾向 C |
| 2 | 唯讀檢視預設顯示哪一版 | 當前發布版？還是有草稿時顯示草稿？ |
| 3 | 版本可以刪除嗎 | 稽核角度傾向不可刪，但測試殘留會累積 |
| 4 | 編輯舊版本後存檔的語意 | 覆寫那一版？還是以它為基礎開新版？ |
| 5 | 誰可以按「開發」 | 目前所有 designer 都可以。要不要更細的權限？ |

### 5.1 第 4 項值得特別想清楚

使用者說「顯示當前表單或流程的所有版本！點選後才能編輯」。

如果點 v1（舊版）進去編輯，存檔時：

- **覆寫 v1** — 破壞了 append-only 的特性，歷史被改寫
- **以 v1 為基礎開新版** — 歷史保持完整，但要說明清楚
  「你正在以 v1 為基礎建立 v4」

**傾向後者**，與需求 03 §3.5 的「還原是往前補一版，不改寫歷史」
一致。

### 5.2 版本差異摘要（可延後）

版本清單如果能顯示「這一版改了什麼」會很有用，
但那需要 diff 邏輯——需求 03 §3.2 已規劃用業務語言顯示差異
（「新增欄位『交貨地點』到『客戶資訊』區塊」）。

**建議不要在本需求實作**，等治理流程那邊做好再複用。
先顯示版本號、狀態、建立者、時間就夠。

---

## 6. 與既有規劃的關係

```
本需求（設計器路徑）
   │
   ├── 依賴：無（後端 API 已完備）
   │
   ├── 交會：D-07 的「編輯開發模式」
   │         兩者都叫「開發」，指同一件事的不同面向：
   │         D-07 = 資料隔離（沙箱租戶）
   │         本文 = 進入路徑（唯讀 → 版本 → 編輯）
   │
   └── 等待：S2 模擬簽核（§3.6，約 3 週後整合）
```

**不衝突，但要協調命名。** 實作時「開發模式」這個詞
在兩處出現，要確認指的是同一個狀態——
使用者按「開發」進入的編輯模式，是否就自動在沙箱裡？

**傾向：是。** 那樣「開發」這個詞才只有一個意思，
使用者也不需要理解兩層概念。但這會讓本需求依賴 S1 沙箱基礎，
不能獨立完成。

需要決定的是：

| 選項 | 說明 |
|------|------|
| **獨立實作** | 先做唯讀／版本／編輯路徑，編輯仍直接改正式租戶。等 S1 完成再接沙箱 |
| **等 S1** | 一次做完，「開發」從一開始就是沙箱 |

**傾向獨立實作。** 唯讀路徑本身就有價值（防誤改、版本可見），
不該被 3 週後的東西擋住。接沙箱是之後的增量改動。

---

## 7. 建議的實作順序

假設選擇獨立實作（§6）：

| 階段 | 內容 | 估計 |
|------|------|------|
| 1 | 補 `workflows.ts` 的版本 API | 0.5 天 |
| 2 | 設計器支援 `readonly` 模式 | 1.5 天 |
| 3 | 路由調整 + 清單點選改為唯讀 | 0.5 天 |
| 4 | 版本清單畫面（表單與流程共用）| 1 天 |
| 5 | 「另存新版」（待 §3.4 決定後）| 1 天 |
| 6 | 更新測試與截圖腳本 | 0.5 天 |
| **合計** | | **約 5 天** |

階段 6 不可省略：路由改動會讓 `auth-navigation.spec.ts` 與
四支截圖腳本失敗，手冊的 76 張截圖也要重產。

---

## 8. 要避免的方向

| 不要做 | 理由 |
|--------|------|
| 另開一個唯讀專用頁面 | 兩份版面必然漂移。擴充既有設計器 |
| 唯讀時隱藏驗證與權限預覽 | 那些是唯讀操作，檢視時很有用 |
| 「另存新版」直接建第二份草稿 | 違反 `form_version_single_draft` 約束 |
| 編輯舊版本時覆寫該版 | 破壞 append-only，歷史被改寫 |
| 等 S2 模擬完成才做 | 唯讀路徑本身有價值，不該被擋 3 週 |
| 唯讀時不標示狀態 | 使用者會以為系統壞了 |
